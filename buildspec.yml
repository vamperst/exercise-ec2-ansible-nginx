version: 0.2
phases:
  install: 
    runtime-versions:
      docker: 18
    commands: 
      - apt-get update -y
      - apt-get install -y jq
      - apt-add-repository ppa:ansible/ansible
      - apt-get update -y
      - apt-get install ansible -y 
      - npm install -g cfn-create-or-update
  pre_build:
    commands:
      - mkdir .ssh && aws s3 cp s3://$BUCKET/instance-need/.ssh/fiap-lab.pem .ssh/
  build:
    commands:
      - cfn-create-or-update --stack-name test-dev --template-body file://ec2-cfn.yaml
      - aws cloudformation --region us-east-1 describe-stacks --stack-name test-dev
      - echo "teste" >> elb.txt
  post_build:
    commands:
      - echo "DONE!!!!"
    
artifacts:
  files: 
    - elb.txt